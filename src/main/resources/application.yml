spring:
  application:
    name: CouponRush
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3307}/coupon?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: ${DB_USER:root}
    password: ${DB_PASS:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 50
      minimum-idle: 10
      connection-timeout: 1000
      validation-timeout: 1000
      max-lifetime: 180000
  transaction:
    default-timeout: 2
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
  endpoint:
    health:
      show-details: always

app:
  features:
    cache: false      # Redis 사용 여부
    messaging: false  # Kafka 사용 여부

server:
  port: 8080
  tomcat:
    threads:
      max: 500

---
# Scenario 1: DB
spring:
  config:
    activate:
      on-profile: scenario1
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

app:
  features:
    cache: false
    messaging: false

---
# Scenario 2: DB + Redis
spring:
  config:
    activate:
      on-profile: scenario2
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  task:
    execution:
      pool:
        core-size: 20
        max-size: 50
        queue-capacity: 500
        keep-alive: 30s

app:
  features:
    cache: true
    messaging: false

---
# Scenario 3: DB + Redis + Kafka
spring:
  config:
    activate:
      on-profile: scenario3
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  kafka:
    bootstrap-servers: ${KAFKA_HOST:localhost}:${KAFKA_PORT:29092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      group-id: coupon-issue-consumer
      auto-offset-reset: earliest
      enable-auto-commit: false
      properties:
        spring.json.trusted.packages: com.apeirogon.rush.messaging
        spring.json.value.default.type: com.apeirogon.rush.messaging.CouponIssueEvent

app:
  features:
    cache: true
    messaging: true